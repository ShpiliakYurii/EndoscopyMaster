var url = "http://localhost:8080/";
function deleteCharacteristic(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteCharacteristic/"+id;
    }
}

function deleteFeature(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteFeature/"+id;
    }
}

function deleteTerm(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteTerm/"+id;
    }
}

function deleteRegion(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteRegion/"+id;
    }
}

function deleteOrgan(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteOrgan/"+id;
    }
}

function deleteRevisionType(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteRevisionType/"+id;
    }
}

function deleteApparatus(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteApparatus/"+id;
    }
}

function deleteDisinfectantSolution(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteDisinfectantSolution/"+id;
    }
}

function deleteRefferal(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteRefferal/"+id;
    }
}

function deleteConclusion(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteConclusion/"+id;
    }
}

function deleteMedicinecard(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteMedicinecard/"+id;
    }
}

function deleteRecomendation(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteRecomendation/"+id;
    }
}

function deleteRecomendations(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteRecomendations/"+id;
    }
}

function deleteManipulation(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteManipulation/"+id;
    }
}

function deleteOperation(id, name){
    if(confirm("Ви впевнені, що хочете видалити "+name+"?")){
        window.location = "/deleteOperation/"+id;
    }
}



